apiVersion : v1
kind: Pod
metadata:
  name: django-app
  labels:
    run: dev
    app: backend
spec:
  containers:
    - name : django-container
      image: django_app
      imagePullPolicy: Never
      ports:
        - containerPort: 80
      
